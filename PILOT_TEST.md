# 多电脑试点验收

当前试点只验证“已有工作台累计升级”，不验证第一次安装。每台电脑使用独立部署票据，不能多人共用一个限时链接。

## 每台电脑固定步骤

1. 记录测试机编号、系统版本、当前工作台版本和真实 Skill 目录；不记录客户姓名、密钥或票据完整 URL。
2. 把固定 GitHub 入口和该电脑自己的部署票据交给 Codex。
3. Codex 先运行 `inspect`，确认没有写入，并说明更新范围、备份边界和阻塞项。
4. 使用者明确回复“同意执行”后才运行 `apply`。
5. 保存 `installed_and_verified` 回执；不要复制签名下载 URL。
6. 重新打开 Codex，做不付费、不上传的业务识别测试。

## 必须通过

| 检查项 | 合格表现 |
| --- | --- |
| 票据隔离 | 每台电脑 ticket_id 不同，过期票据会被拒绝 |
| 平台 | Mac 票据不能装 Windows，Windows 票据不能装 Mac |
| 版本 | 目标版本、Release tag、包 SHA-256 与不可变清单一致 |
| 安装目录 | 工作台和 Skill 目录是实际目标；出现双目录歧义时停止 |
| 备份 | 回执能打开对应 `backup_record.json` |
| 指纹 | `post_install_tree_verification=passed`，v1.4.2 为 177 项 |
| 数据保护 | 历史项目、素材、成果和个人配置仍在原处 |
| 重启识别 | 重新打开 Codex 后能识别图文、视频和本次重点工作流 |
| 费用与上传 | 部署过程 `paid_calls=0`、`external_uploads=0` |

## 失败即停止

- 下载文件大小或 SHA-256 不一致。
- 找不到现有工作台，或同时存在多个可能的 Skill 目录。
- 安装后指纹不是 `passed` 或不是预期数量。
- 历史项目、素材、成果或配置出现异常变化。
- Windows PowerShell 权限、编码、路径或安全软件阻止执行。

失败时保留回执、备份记录和不含密钥的错误摘要，不自动回滚。需要回滚时先说明会恢复哪些文件，再单独取得明确确认。

## 汇总

收集各电脑的部署回执到一个仅内部可见的目录后运行：

```text
python scripts/summarize_receipts.py <回执1.json> <回执2.json> ...
```

只有 Mac 和 Windows 真实电脑都通过，才允许把 `pilot` 晋升为 `stable`。

