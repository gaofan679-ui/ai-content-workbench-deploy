# Codex 部署执行规范

收到本页和部署票据后，按以下顺序执行。不要让使用者复制长命令，也不要在聊天中回显票据完整 URL、API Key、Cookie 或下载签名参数。

## 部署前安全边界

环境检查、票据读取和安装都不需要扫描系统钥匙串或浏览器安全存储。禁止运行全量钥匙串导出或扫描命令（例如 macOS 的 `security dump-keychain`），也不得读取浏览器 Cookie、登录态或 `Doubao Browser Safe Storage`。确实需要查询部署方已经登记的 COS 凭据时，只能按明确的服务名和账户名精确查询，设置超时，并在结束后确认没有残留诊断进程。

如果 macOS 弹出钥匙串密码或浏览器安全存储授权窗口，立即停止触发弹窗的诊断命令；不要要求使用者输入密码，也不要让使用者反复点击“允许”或“始终允许”。改用不读取浏览器密钥的网络、票据和配置检查路线继续部署。缺少凭据时直接报告“需要补充配置”，不能扩大扫描范围。

## 第一步：只读检查（不需要客户确认）

1. 下载或读取本仓库当前版本的 `scripts/deploy.py`。
2. 询问部署票据链接；只在本机进程参数中使用，不完整回显。
3. 识别操作系统、工作台目录和 Skill 目录。
4. 同时检查 `.codex/skills` 与 `.agents/skills`。如果两处都有工作台管理的同名能力，不把技术选择题交给使用者：优先使用历史部署回执明确记录的目录；没有回执时选择工作台能力更完整的目录，相同数量时使用标准默认目录。另一处只同步其中已经存在的同名工作台能力，先逐项备份，不新增第二套完整副本，不处理个人工作流。
5. 直接运行只读检查。只读检查不会下载客户包、不会写入工作台，也不需要使用者先确认。v1.5.3 自动识别票据不要求使用者先选择“首次安装”或“升级”：

```text
python deploy.py inspect --ticket <部署票据> --workbench <工作台目录> --skills-home <真实Skill目录>
```

6. 自动识别规则：空工作台＝首次安装；完整工作台＝升级。只有部分工作流残留时先备份再补齐；工作台存在但工作流缺失时自动重建。两处标准工作流目录并存时按上一条备份并同步。只有两个都包含真实项目数据的工作台、工作台清单不唯一/越界、软链接或无法证明安全的数据冲突才停止。
7. 同时读取匹配版本清单的环境要求；先查 PATH，再查常见安装位置。缺少必需组件时，先判断当前电脑是否已有官方安装工具（Windows 的 winget、Mac 的 Homebrew）。只要具备官方安装工具，就把“自动补齐基础环境并继续安装”纳入一次确认；检查阶段仍不下载、不写入。
8. 用大白话报告：判断的安装模式、目标版本、将更新什么、环境是否满足、预计备份位置、哪些历史内容不会动。报告完成后，只向使用者请求一次“同意执行”确认；这次确认同时覆盖支持的基础环境安装和工作台写入。

### 自动网络回退（先系统工具，后浏览器）

如果 Python 的 HTTPS 读取票据或客户包失败，安装器会先调用系统自带的 `curl`。Windows 的系统版 curl 使用系统证书通道，常见的 Python 证书错误不需要客户处理；只有 curl 也失败时，Windows 才自动尝试调用本机 Edge 或 Chrome：

```text
直接读取失败
→ 自动调用系统网络工具读取票据或下载客户包
→ Windows 仍失败时自动调用 Edge/Chrome
→ 票据校验通过后，只请求一次确认
→ 下载完成后校验 SHA-256
→ 所有自动通道失败才停止并说明原因
```

客户不需要打开浏览器、寻找文件、下载 JSON、上传票据或处理 ZIP。只有系统网络工具和系统浏览器都被安全软件或网络策略阻止时，才需要服务方介入排障。固定入口主站打不开时，使用同一不可变版本的 jsDelivr 备用入口，不把客户包或票据放到公开镜像。

### Codex 有界自主恢复

一次“同意执行”后，Codex 不应在每个可恢复异常处停下来询问使用者。按下面的固定边界持续处理，直到安装验收通过或命中真正需要人工处理的安全边界：

```text
基础工具缺失 → 使用官方系统安装工具补齐 → 重新体检
临时网络失败 → 直接读取、系统网络工具、不可变备用入口、系统浏览器依次尝试
两处标准工作流目录 → 选定主目录 → 分别备份 → 只同步已存在的同名工作台能力 → 重新验收
部分工作流残留 → 备份残留 → 补齐完整工作台 → 重新验收
安装进程中断 → 核对回执、备份和文件身份 → 未产生写入时重跑；已有写入时从确定性安装器继续
Mac 文件与工作流已校验、仅常驻服务未就绪 → 从服务注册断点继续 → 不重复安装、不新增第二份升级备份
```

同一类免费、幂等修复最多尝试两次。第二次仍失败，或两个以上入口出现相同的无响应故障时，先检查共同的网络、代理、证书或权限根因，不继续盲目重试。不得为了“自动完成”而修改 VPN、代理、DNS、注册表、杀毒软件或系统安全策略。

部署总控会把已确认任务的进度写入本机专用断点，只记录票据编号、目标版本、包哈希、执行阶段、错误指纹和恢复决定，不保存完整票据链接、签名下载参数、API Key、Cookie 或登录态。客户包下载并通过哈希验证后进入本机部署缓存；同一票据继续执行时优先复用已验证文件，不重复下载。

如果执行意外中断，Codex 先读取断点状态：

```text
python deploy.py status --ticket <部署票据> --workbench <工作台目录> --skills-home <真实Skill目录>
```

状态明确为 `can_resume_automatically: true` 时，说明仍在第一次“同意执行”的授权范围内，Codex 直接运行：

```text
python deploy.py resume --ticket <部署票据> --workbench <工作台目录> --skills-home <真实Skill目录>
```

不要再次询问客户，也不要让客户处理命令、JSON 或 ZIP。`resume` 只允许同一票据、同一版本、同一包哈希，并且错误已经登记为免费、幂等、可安全重试时使用；没有原始确认断点或错误不在安全恢复范围内时会拒绝继续。

固定入口中的恢复规则库负责把错误分成：网络临时问题、基础依赖问题、本人授权、真实数据冲突、安全校验失败、正式包缺陷和未知问题。网络与基础依赖按登记动作自动恢复；正式包缺陷不得在客户电脑上偷偷篡改不可变包，必须更换新版本。Codex 应把技术证据留在内部断点，只给客户显示大白话结论。

只有以下情况可以暂停并要求使用者配合：系统密码或管理员授权弹窗；客户自己的账号登录、验证码和付费确认；两个工作台都包含无法自动取舍的真实项目数据；清单越界、哈希不一致、软链接或权限策略明确阻止安装。普通错误码、目录名和脚本名不得直接抛给使用者，应保留在内部回执并给出业务化结论。

## 第二步：获得唯一一次写入确认

只有只读检查完成后，使用者明确回复“同意执行”，才能运行写入。这个确认会覆盖：支持的基础工具自动安装、工作台包下载与校验、备份、安装和安装后验收。不要在只读检查前重复索要确认，也不要把“查看”“检查”或“你看着办”当成写入授权。Windows 或 Mac 可能仍会出现系统权限弹窗，必须由使用者在系统弹窗中允许；Codex 不会绕过系统权限。

## 第三步：下载、校验、备份和安装

```text
python deploy.py apply --ticket <部署票据> --workbench <工作台目录> --skills-home <真实Skill目录> --confirm-write YES
```

安装器会依次：

```text
检查票据有效期与目标平台
→ 自动选择当前系统和安装模式对应的不可变版本清单
→ 检查当前版本要求的本地组件
→ 如缺少且系统已有官方安装工具，自动安装缺少的基础工具并刷新 PATH
→ 重新体检；未通过时不继续写入工作台
→ 下载客户包到临时目录
→ 校验文件大小和 SHA-256
→ 安全解压并阻止越界路径/软链接
→ 运行升级包只读检查
→ 由升级包备份本次会更新的文件
→ 安装
→ 核对安装后文件指纹
→ 生成部署验收回执
```

## 第四步：验收

安装完成不等于业务验收完成。至少检查：

1. 回执状态为 `installed_and_verified`。
2. 回执中 `post_install_tree_verification` 为 `passed`。
3. 回执中保存了本次环境体检结果。
4. 重新打开 Codex。
5. 用一句日常话分别识别图文、视频和本次重点更新的工作流。
6. 视频工作流在上传或付费调用前再次运行自己的启动检查。
7. 外部模型或付费能力只做免费连接检查；真实生成必须另行说明费用和隐私边界。

## 第五步：给使用者的回执

不要把原始 JSON、命令输出或内部验收字段直接展示给使用者。客户版回执固定使用下面 5 段：

```text
当前结论：已完成 / 暂停 / 需要补充
这次完成了什么：最多 4 条业务变化
原有内容是否保留：项目、素材、成果和配置分别说明
还需要你做什么：最多 3 个动作，按顺序写
从哪里继续：给出可点击的入口名称或一句能直接发送的话
```

客户版回执不要主动出现：`installed_and_verified`、`blocked_environment`、Skill 数量、文件哈希、内部备份路径、票据编号、脚本名、错误码或完整工作台路径。只有使用者明确要求技术细节，或技术问题确实阻塞下一步时，才补充必要信息。

升级完成后，优先引导使用者打开工作台根目录中的“AI 内容工作台｜从这里开始”入口。这个入口会解释新旧目录的对应关系，不要求使用者手动整理、改名或移动旧文件。

## 失败处理

- 票据过期：向服务方申请新票据，不修改电脑时间。
- 下载失败：先自动尝试系统 curl；Windows 再自动尝试 Edge/Chrome 一次；仍失败时保留业务化错误摘要，不回显完整签名 URL，交由服务方排障。
- 基础环境缺失：优先自动调用 Windows winget 或 Mac Homebrew，按工具逐项有界重试并重新体检；安装工具不存在、权限被拒绝或重试后仍缺失时停止，不修改代理、DNS、注册表、杀毒软件或系统安全策略。
- 哈希不一致：立即停止，不解压、不安装。
- 安装目标不明确：停止并让使用者或服务方确认真实目录。
- 两处标准工作流目录：自动选择主目录，分别备份，只同步另一处已经存在的同名工作台能力；个人工作流保持原样。
- 工作台或工作流只发现一部分：按可恢复残留处理，备份后补齐并重新验收；只有真实项目数据冲突、清单不安全、软链接或无写入权限时才停止。
- 安装失败：读取升级包的备份记录和错误摘要；不要自动回滚。
- 意外中断或可恢复失败：先运行 `status`；只有明确允许自动恢复时运行 `resume`，沿用第一次确认，不再询问客户。
- 正式包缺陷：停止使用同一包和同一版本；不要在客户电脑上修改已校验包，交由部署方发布新的不可变版本。
- 需要回滚：先展示将恢复的备份记录，等使用者再次明确确认后再执行。
